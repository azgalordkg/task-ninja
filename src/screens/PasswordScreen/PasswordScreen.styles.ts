import { StyleSheet } from "react-native";

const styles = () =>
  StyleSheet.create({
    container: {
      flex: 1,
      paddingVertical: 30,
    },
    content: {
      flexGrow: 1,
      rowGap: 30,
    },
  });

export default styles;
