import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonContainer: {
    marginHorizontal: 20,
    marginTop: 8,
  },
  emptyContainer: {
    paddingTop: 40,
  },
});

export default styles;
